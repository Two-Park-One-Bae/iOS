// Copy this file to Secrets.xcconfig and fill in the values.
// Secrets.xcconfig is gitignored and must never be committed.

AMPLITUDE_API_KEY =
DEVELOPMENT_TEAM =
BASE_URL_DEBUG =
BASE_URL_RELEASE =
// App Check 디버그 토큰(개발용). Firebase Console → App Check → 디버그 토큰 관리에서 발급/등록한 UUID.
// 개발 빌드(시뮬·실기기)에서 App Check 디버그 프로바이더가 이 값을 써서 재설치·초기화에도 고정된다.
APP_CHECK_DEBUG_TOKEN =

// 소셜 로그인 (NM-410)
// 카카오 디벨로퍼스 → 내 애플리케이션 → 앱 키 → 네이티브 앱 키.
// SDK 초기화와 URL 스킴(kakao{키})에 함께 쓰인다.
KAKAO_NATIVE_APP_KEY =
// GoogleService-Info.plist 의 REVERSED_CLIENT_ID 를 그대로 옮긴다(구글 로그인 복귀 URL 스킴).
// Firebase 콘솔에서 Google 공급자를 켜야 plist 에 이 키가 생긴다 — dev/prod 값이 다르다.
GOOGLE_REVERSED_CLIENT_ID_DEBUG =
GOOGLE_REVERSED_CLIENT_ID_RELEASE =
